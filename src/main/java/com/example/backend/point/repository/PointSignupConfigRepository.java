package com.example.backend.point.repository;

import com.example.backend.point.entity.PointSignupConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PointSignupConfigRepository extends JpaRepository<PointSignupConfig, Long> {
}